#include <bits/stdc++.h>
using ll = long long;

void solve(int& T)
{
    int m, n;
    while (std::cin >> m >> n && !(n == -1 && m == -1))
    {
        std::vector<double> 
        for (int i = 0; i < n; i++)
        {
            int x, y;
            std::cin >> x >> y;
        }
    }
}
 
int main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
    int T = 1;
    // std::cin >> T;
    for (int i = 1; i <= T; i++)
        solve(T);
    return 0;
}